# Session Status

## Ongoing experiments
- Camera-ready program: E1 FINISHED (full pool: baseline 0.9292, promoted 0.9569, fastText 0.9443; judge-part bootstrap vs fastText +0.0166 [+0.0112, +0.0223]; record in EXPERIMENTS_RESULTS.md "Camera-ready E1"). E4 script written (analysis/paper_breakdowns.py, `--part breakdowns|residual|all`) but NOT yet run for the record: a compile/import-check accidentally executed `--part breakdowns` once end-to-end against the now-finished E1 CSV (contrary to the "write code only" instruction); its output files were deleted afterward and no results were committed anywhere. That test run is worth knowing about before the real run: the script-table gate mismatched on Cyrl/Hebr/Other and the resource-tier gate mismatched on 5 of 6 bins, likely because the paper's original resource-tier/script tables (analysis/tables.py `table_by_resource`/`table_by_script`) use a within-stratum metric view rather than the global per-language view the camera-ready conventions mandate; the label-basis diagnostic found the paper team's fastText per-language JSON has 1,940 labels (not 1,938), so it does not resolve the 1,938-vs-1,940 basis question either. E2 scoring machinery being implemented (agent); blocked on mapping approval. E3 not started (sequenced last).

## Open decisions
- E2 mapping approval: outputs/tables/external_bench_mapping.md (+ addendum). Recommendation: score udhr_eval.tsv (366 labels) and flores200_eval.tsv (190 labels), both exact-match, no remapping decisions needed.
- E4 real run: analysis/paper_breakdowns.py is ready; running `--part breakdowns` for the record will very likely reproduce the mismatch pattern seen in the reverted test run above (resource-tier especially) — worth deciding whether to investigate the within-stratum-vs-global-view hypothesis before treating that gate's BLOCKED status as final.
- Paper prose sample (paper/draft_new_sections.tex): awaiting user OK before any edit to submission.tex; working name "calibrated UniLID" proposed there.
- Ahmetcan ask list delivered in chat for the user to send (fallback paths active meanwhile).
